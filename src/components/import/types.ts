/**
 * Shared UI types for the URL/HTML import modal and its subcomponents.
 */

export type ImportStatus = 'idle' | 'fetching' | 'preview' | 'analyzing' | 'generating' | 'done' | 'error' | 'prompt_edit'
